import { Button } from "@mui/material";
import { styled } from "@mui/material/styles";

const PrimaryTextButton = styled(Button)(({ theme }) => ({
  color: "#b10b1c",
}));

export default PrimaryTextButton;
